{
  "name": "subgraph",
  "license": "UNLICENSED",
  "packageManager": "yarn@4.2.2",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ nnsprotocol/nns",
    "create-local": "graph create --node http://localhost:8020/ nnsprotocol/nns",
    "remove-local": "graph remove --node http://localhost:8020/ nnsprotocol/nns",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 --version-label v0.0.1 nnsprotocol/nns",
    "deploy-sepolia": "yarn codegen && yarn build && goldsky subgraph delete -f nns/0.0.2 && goldsky subgraph deploy nns/0.0.2 --path .",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.73.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
